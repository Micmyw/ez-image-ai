# 2026-09-14：现有生产模型的目录兼容性复核

## 结论和适用范围

Cloudflare 原生 Git 构建已连接 `main`。本次发布被整体目录版本校验拦截：生产配置保留
`2026-09-07.2`，新增三个模型后的目录版本为 `2026-09-13.1`。原有四个已启用模型的
供应商接口、输入参数、输出规格和供应商成本均未变化，因此本次发布保留这四个模型的
原有启用范围。兼容规则仅适用于这两个明确版本，不自动延伸到后续目录版本。

生产配置中的认证列表仍为 `2026-09-07.2`。新模型以及未列入本次复核的其他模型，必须
满足当前版本验收条件才能执行；仅打开它们的功能开关，不能借用本次兼容规则。
网站运行时和发布前检查使用同一个判断函数，防止构建通过后模型列表变空。

## 真实运行记录

2026-09-14 05:25–05:31 UTC 对正式 PostgreSQL 执行只读查询，并重新向 Kie 查询既有任务。
以下四个任务均满足：应用任务成功、供应商任务成功、一个 READY 私有输出、一次积分结算、
结算额等于原预留额。Kie 的四次查询均返回 HTTP 200、`state=success` 和一个输出；
输出主机为 `tempfile.aiquickdraw.com`，未在文档中记录私有图片地址或凭据。

| 产品               | 实测输出规格 | 成功任务                    | 当时结算积分 |
| ------------------ | ------------ | --------------------------- | ------------ |
| Nano Banana 2 Lite | 1K           | `cmty47vnr0001psp7d0xt18iu` | 5            |
| Seedream 4.5       | 2K Basic     | `cmty5taqy0003psp7s2aji13p` | 8            |
| Seedream 5 Lite    | 4K Ultra     | `cmty63bol0001psp7hsotlo7j` | 7            |
| Seedream 5 Pro     | 1K Basic     | `cmty690ib0001psp798lj0e8k` | 8            |

这些任务于 2026-09-12 完成。数据库还保留一个 `NO_USABLE_OUTPUT` 失败任务，其预留的
5 积分已全部释放，结算额为 0。审核记录由真实 `sightengine` 适配器产生。

完整只读查询结果保存在忽略目录 `output/model-catalog-sync/production-model-evidence.json`
和 `provider-record-evidence.json`。查询没有新建任务、修改账本或产生新的供应商调用费用。
供应商记录接口未返回实际扣费字段；数据库 `providerCostMicros` 不能单独作为账单凭证。

## 代码兼容范围

基线提交：`000d7753b8957e1715fcdc8f13ee22f8dd89fba0`。从该提交提取了四个模型的全部
8 个 SKU、比例、输出控制、路由、成本和供应商请求定义，保存为
`packages/ai/media/catalog/fixtures/kie-production-provider-contract-2026-09-07.2.json`。

回归检查把当前代码实际生成的请求与旧提交的固定数据对比，覆盖每个合法比例和输出控制。
原有请求、轮询和结果处理保持兼容。此次目录扩展另外调整了三档原有零售积分：Seedream 4.5
4K 为 12，Seedream 5 Lite 3K 为 10、4K 为 14；供应商成本未变。报价仍使用新价格版本
`2026-09-13.2`，历史任务保持其原有报价和结算快照。

## 验证边界

真实执行记录覆盖上表四个 SKU；其余四档输出的新增真实付费复测、供应商账单对账、不确定提交
恢复、审核拒绝以及回滚演练仍未在本记录中完成。固定合同检查使用本地模拟响应，证明代码
兼容性，不替代这些外部验收。`ezpic-staging-evidence.json` 的未完成场景保持原状。

本次规则用于保留已经启用的生产行为。新增产品、供应商合同变化和后续目录版本需要重新复核，
不得复制此兼容列表或自动填写新的认证版本。
