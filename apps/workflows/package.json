{
	"name": "@repo/workflows",
	"private": true,
	"type": "module",
	"scripts": {
		"type-check": "tsc --noEmit --incremental false",
		"types": "wrangler types",
		"build": "wrangler deploy --dry-run --outdir dist",
		"build:workers": "wrangler deploy --config wrangler.workers.jsonc --dry-run --outdir dist-workers",
		"test:artifact:workerd": "node test-support/artifact-workerd-smoke.mjs",
		"dev": "wrangler dev",
		"test": "vitest run"
	},
	"dependencies": {
		"@cloudflare/containers": "catalog:",
		"@repo/database": "workspace:*",
		"@repo/jobs": "workspace:*",
		"@repo/storage": "workspace:*"
	},
	"devDependencies": {
		"@cloudflare/vitest-plugin": "1.1.5",
		"@repo/tsconfig": "workspace:*",
		"@types/node": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:",
		"wrangler": "catalog:"
	}
}
